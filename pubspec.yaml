name: theme_toggle
description: A customized Theme toggle with emoji.
version: 0.0.1
homepage: https://github.com/yahu1031/theme-toggle.git

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: